<div class="md-shell {{if (is-equal this.activeTab "boards") "md-shell--board-view"}} {{if (is-equal this.activeTab "reports") "md-shell--reports-view"}} {{if (is-equal this.activeTab "extras") "md-shell--extras-view"}} {{if (is-equal this.activeTab "supervisors") "md-shell--supervisor-view"}}">
  {{!-- Primary Nav Pills (navbar is in application header when on modern-dashboard) --}}
  <nav class="md-pillnav" aria-label={{t "Primary Navigation" key="primary_navigation"}}>
    <button type="button" class="md-pillnav__pill {{if (is-equal this.activeTab "home") "is-active"}}" {{action "goTab" "home"}}>
      {{t "Home" key="home"}}
    </button>
    <button type="button" class="md-pillnav__pill {{if (is-equal this.activeTab "boards") "is-active"}}" {{action "goTab" "boards"}}>
      {{t "Boards" key="boards"}}
    </button>
    <button type="button" class="md-pillnav__pill {{if (is-equal this.activeTab "reports") "is-active"}}" {{action "goTab" "reports"}}>
      {{t "Reports" key="reports"}}
    </button>
    <button type="button" class="md-pillnav__pill {{if (is-equal this.activeTab "extras") "is-active"}}" {{action "goTab" "extras"}}>
      {{t "Extras" key="extras"}}
    </button>
    <button type="button" class="md-pillnav__pill {{if (is-equal this.activeTab "supervisors") "is-active"}}" {{action "goTab" "supervisors"}}>
      {{t "Supervisors" key="supervisors"}}
    </button>
  </nav>

  <main class="md-main">
    <header class="md-hero md-hero--dashboard">
      <h1 class="md-hero__title">{{#if this.appState.currentUser}}{{t "It's great to see you %{username}" key="md_hero_greeting" username=this.appState.currentUser.user_name}}{{else}}{{t "It's great to see you" key="md_hero_greeting"}}{{/if}}</h1>
      <p class="md-hero__sub">{{t "Let's continue where you left off." key="md_hero_sub"}}</p>
    </header>

    <section class="md-grid md-grid--dashboard" aria-label={{t "Dashboard" key="dashboard"}}>
      <article class="md-card md-card--speak" aria-label={{t "Speak Mode" key="speak_mode"}}>
        <div class="md-card__head">
          <div class="md-card__icon md-card__icon--speak" aria-hidden="true">
            {{!-- Modern mic SVG (switch back by uncommenting and removing img):
            <svg class="md-card__icon-svg md-card__icon-svg--mic" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="26" height="26" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 2a3 3 0 0 1 3 3v7a3 3 0 0 1-6 0V5a3 3 0 0 1 3-3Z"/><path d="M19 10v2a7 7 0 0 1-14 0v-2"/><line x1="12" x2="12" y1="19" y2="22"/></svg>
            --}}
            <img src={{path "images/icon-sound.png"}} class="md-card__icon-img" alt="" aria-hidden="true">
          </div>
          <div class="md-card__titles">
            <div class="md-card__title">{{t "Speak Mode" key="speak_mode"}}</div>
            <div class="md-card__sub">{{t "Start communication" key="start_communication"}}</div>
          </div>
        </div>
        <div class="md-card__row">
          <div class="md-chip md-chip--soft">
            <span class="md-chip__dot" aria-hidden="true"></span>
            {{t "Last board: Beginner Lessons" key="md_last_board"}}
          </div>
          <button type="button" class="md-btn md-btn--primary" {{action "go" "speak"}}>
            {{t "Continue Speaking" key="continue_speaking"}}
          </button>
        </div>
        <div class="md-card__bg md-card__bg--wave" aria-hidden="true"></div>
        <img src={{path "images/sound-waves.png"}} class="md-card__sound-waves" alt="" aria-hidden="true">
      </article>

      <article class="md-card md-card--extras" aria-label={{t "Extras" key="extras"}}>
        <div class="md-card__head md-card__head--between">
          <div>
            <div class="md-card__title">{{t "Extras" key="extras"}}</div>
            <div class="md-card__sub">{{t "Games & Resources" key="games_and_resources"}}</div>
          </div>
        </div>
        <div class="md-extrasRow">
          <div class="md-extrasIcon" aria-hidden="true">
            {{!-- Grid SVG (switch back by uncommenting and removing img):
            <svg class="md-extrasIcon__svg" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="26" height="26" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect width="9" height="9" x="3" y="3" rx="2"/><rect width="9" height="9" x="12" y="3" rx="2"/><rect width="9" height="9" x="3" y="12" rx="2"/><rect width="9" height="9" x="12" y="12" rx="2"/></svg>
            --}}
            <img src={{path "images/icon-extras.png"}} class="md-extrasIcon__img" alt="" aria-hidden="true">
          </div>
          <button type="button" class="md-btn md-btn--primary" {{action "goTab" "extras"}}>
            {{t "Explore Extras" key="explore_extras"}}
          </button>
        </div>
        <img src={{path "images/extras-wave.png"}} class="md-card__extras-wave" alt="" aria-hidden="true">
      </article>

      {{#if (is-equal this.activeTab "boards")}}
      <div class="md-boards-page" aria-label={{t "Boards" key="boards"}}>
        <div class="md-card__head md-card__head--between {{if this.showNewBoardForm "md-card__head--center-back"}}">
          <div class="md-card__left">
            <div class="md-card__title">{{t "Boards" key="boards"}}</div>
            <div class="md-card__sub">{{t "Manage your boards" key="manage_your_boards"}}</div>
          </div>
          {{#if this.showNewBoardForm}}
          <button type="button" class="md-btn md-btn--primary" {{action "closeNewBoardForm"}}>{{t "Back to Boards" key="back_to_boards"}}</button>
          {{else}}
          <button type="button" class="md-btn md-btn--primary" {{action "openNewBoardOnBoards"}}>{{t "New Board" key="new_board"}}</button>
          {{/if}}
          <div class="md-stat">
            <div class="md-stat__num">12</div>
            <div class="md-stat__meta">
              <div>{{t "Total Boards" key="total_boards"}}</div>
              <div class="md-muted">{{t "8 Pinned" key="eight_pinned"}}</div>
            </div>
            <img src={{path "images/icon-boards.png"}} class="md-card__boards-icon" alt="" aria-hidden="true">
          </div>
        </div>
        {{#if this.showNewBoardForm}}
        <div class="md-card__new-board-inline">
          {{new-board standalone=true onClose=(action "closeNewBoardForm")}}
        </div>
        {{else}}
        <div class="md-strip" role="list" aria-label={{t "Board previews" key="board_previews"}}>
          <button type="button" class="md-strip__item" role="listitem" {{action "go" "boards"}}>
            <div class="md-thumb md-thumb--a md-thumb--with-icon" aria-hidden="true">
              <img src={{path "images/pastel-chat.svg"}} class="md-thumb__icon" alt="">
            </div>
            <div class="md-strip__label">{{t "Make Small Talk" key="make_small_talk"}}</div>
          </button>
          <button type="button" class="md-strip__item" role="listitem" {{action "go" "boards"}}>
            <div class="md-thumb md-thumb--b md-thumb--with-icon" aria-hidden="true">
              <img src={{path "images/pastel-getting-started.svg"}} class="md-thumb__icon" alt="">
            </div>
            <div class="md-strip__label">{{t "Beginner Lessons" key="beginner_lessons"}}</div>
          </button>
          <button type="button" class="md-strip__item" role="listitem" {{action "go" "boards"}}>
            <div class="md-thumb md-thumb--c md-thumb--with-icon" aria-hidden="true">
              <img src={{path "images/pastel-sound.png"}} class="md-thumb__icon" alt="">
            </div>
            <div class="md-strip__label">{{t "Conversation…" key="conversation_ellipsis"}}</div>
          </button>
          <button type="button" class="md-strip__item" role="listitem" {{action "go" "boards"}}>
            <div class="md-thumb md-thumb--d md-thumb--with-icon" aria-hidden="true">
              <img src={{path "images/pastel-lightbulb.png"}} class="md-thumb__icon" alt="">
            </div>
            <div class="md-strip__label">{{t "Board 4" key="board_4"}}</div>
          </button>
          <button type="button" class="md-strip__item" role="listitem" {{action "go" "boards"}}>
            <div class="md-thumb md-thumb--e md-thumb--with-icon" aria-hidden="true">
              <img src={{path "images/pastel-house.png"}} class="md-thumb__icon" alt="">
            </div>
            <div class="md-strip__label">{{t "Board 5" key="board_5"}}</div>
          </button>
          <button type="button" class="md-strip__item" role="listitem" {{action "go" "boards"}}>
            <div class="md-thumb md-thumb--f md-thumb--with-icon" aria-hidden="true">
              <img src={{path "images/pastel-logging.png"}} class="md-thumb__icon" alt="">
            </div>
            <div class="md-strip__label">{{t "Board 6" key="board_6"}}</div>
          </button>
          <button type="button" class="md-strip__item" role="listitem" {{action "go" "boards"}}>
            <div class="md-thumb md-thumb--a md-thumb--with-icon" aria-hidden="true">
              <img src={{path "images/pastel-reports2.png"}} class="md-thumb__icon" alt="">
            </div>
            <div class="md-strip__label">{{t "Board 7" key="board_7"}}</div>
          </button>
          <button type="button" class="md-strip__item" role="listitem" {{action "go" "boards"}}>
            <div class="md-thumb md-thumb--b md-thumb--with-icon" aria-hidden="true">
              <img src={{path "images/pastel-extras.png"}} class="md-thumb__icon" alt="">
            </div>
            <div class="md-strip__label">{{t "Board 8" key="board_8"}}</div>
          </button>
          <button type="button" class="md-strip__item" role="listitem" {{action "go" "boards"}}>
            <div class="md-thumb md-thumb--c md-thumb--with-icon" aria-hidden="true">
              <img src={{path "images/pastel-modeling.png"}} class="md-thumb__icon" alt="">
            </div>
            <div class="md-strip__label">{{t "Board 9" key="board_9"}}</div>
          </button>
          <button type="button" class="md-strip__item" role="listitem" {{action "go" "boards"}}>
            <div class="md-thumb md-thumb--d md-thumb--with-icon" aria-hidden="true">
              <img src={{path "images/pastel-star.svg"}} class="md-thumb__icon" alt="">
            </div>
            <div class="md-strip__label">{{t "Board 10" key="board_10"}}</div>
          </button>
          <button type="button" class="md-strip__item" role="listitem" {{action "go" "boards"}}>
            <div class="md-thumb md-thumb--e md-thumb--with-icon" aria-hidden="true">
              <img src={{path "images/browse-boards-icon.svg"}} class="md-thumb__icon" alt="">
            </div>
            <div class="md-strip__label">{{t "Board 11" key="board_11"}}</div>
          </button>
          <button type="button" class="md-strip__item" role="listitem" {{action "go" "boards"}}>
            <div class="md-thumb md-thumb--f md-thumb--with-icon" aria-hidden="true">
              <img src={{path "images/icon-boards.png"}} class="md-thumb__icon" alt="">
            </div>
            <div class="md-strip__label">{{t "Board 12" key="board_12"}}</div>
          </button>
        </div>
        {{/if}}
      </div>
      {{else}}
      <article class="md-card md-card--boards" aria-label={{t "Boards" key="boards"}}>
        <div class="md-card__head md-card__head--between">
          <div class="md-card__left">
            <div class="md-card__title">{{t "Boards" key="boards"}}</div>
            <div class="md-card__sub">{{t "Manage your boards" key="manage_your_boards"}}</div>
          </div>
          <button type="button" class="md-btn md-btn--primary" {{action "openNewBoardOnBoards"}}>{{t "New Board" key="new_board"}}</button>
          <div class="md-stat">
            <div class="md-stat__num">12</div>
            <div class="md-stat__meta">
              <div>{{t "Total Boards" key="total_boards"}}</div>
              <div class="md-muted">{{t "8 Pinned" key="eight_pinned"}}</div>
            </div>
            <img src={{path "images/icon-boards.png"}} class="md-card__boards-icon" alt="" aria-hidden="true">
          </div>
        </div>
        <div class="md-strip" role="list" aria-label={{t "Board previews" key="board_previews"}}>
          <button type="button" class="md-strip__item" role="listitem" {{action "go" "boards"}}>
            <div class="md-thumb md-thumb--a md-thumb--with-icon" aria-hidden="true">
              <img src={{path "images/pastel-chat.svg"}} class="md-thumb__icon" alt="">
            </div>
            <div class="md-strip__label">{{t "Make Small Talk" key="make_small_talk"}}</div>
          </button>
          <button type="button" class="md-strip__item" role="listitem" {{action "go" "boards"}}>
            <div class="md-thumb md-thumb--b md-thumb--with-icon" aria-hidden="true">
              <img src={{path "images/pastel-getting-started.svg"}} class="md-thumb__icon" alt="">
            </div>
            <div class="md-strip__label">{{t "Beginner Lessons" key="beginner_lessons"}}</div>
          </button>
          <button type="button" class="md-strip__item" role="listitem" {{action "go" "boards"}}>
            <div class="md-thumb md-thumb--c md-thumb--with-icon" aria-hidden="true">
              <img src={{path "images/pastel-sound.png"}} class="md-thumb__icon" alt="">
            </div>
            <div class="md-strip__label">{{t "Conversation…" key="conversation_ellipsis"}}</div>
          </button>
          <button type="button" class="md-strip__item" role="listitem" {{action "go" "boards"}}>
            <div class="md-thumb md-thumb--d md-thumb--with-icon" aria-hidden="true">
              <img src={{path "images/pastel-lightbulb.png"}} class="md-thumb__icon" alt="">
            </div>
            <div class="md-strip__label">{{t "Board 4" key="board_4"}}</div>
          </button>
          <button type="button" class="md-strip__item" role="listitem" {{action "go" "boards"}}>
            <div class="md-thumb md-thumb--e md-thumb--with-icon" aria-hidden="true">
              <img src={{path "images/pastel-house.png"}} class="md-thumb__icon" alt="">
            </div>
            <div class="md-strip__label">{{t "Board 5" key="board_5"}}</div>
          </button>
          <button type="button" class="md-strip__item" role="listitem" {{action "go" "boards"}}>
            <div class="md-thumb md-thumb--f md-thumb--with-icon" aria-hidden="true">
              <img src={{path "images/pastel-logging.png"}} class="md-thumb__icon" alt="">
            </div>
            <div class="md-strip__label">{{t "Board 6" key="board_6"}}</div>
          </button>
          <button type="button" class="md-strip__item" role="listitem" {{action "go" "boards"}}>
            <div class="md-thumb md-thumb--a md-thumb--with-icon" aria-hidden="true">
              <img src={{path "images/pastel-reports2.png"}} class="md-thumb__icon" alt="">
            </div>
            <div class="md-strip__label">{{t "Board 7" key="board_7"}}</div>
          </button>
          <button type="button" class="md-strip__item" role="listitem" {{action "go" "boards"}}>
            <div class="md-thumb md-thumb--b md-thumb--with-icon" aria-hidden="true">
              <img src={{path "images/pastel-extras.png"}} class="md-thumb__icon" alt="">
            </div>
            <div class="md-strip__label">{{t "Board 8" key="board_8"}}</div>
          </button>
          <button type="button" class="md-strip__item" role="listitem" {{action "go" "boards"}}>
            <div class="md-thumb md-thumb--c md-thumb--with-icon" aria-hidden="true">
              <img src={{path "images/pastel-modeling.png"}} class="md-thumb__icon" alt="">
            </div>
            <div class="md-strip__label">{{t "Board 9" key="board_9"}}</div>
          </button>
          <button type="button" class="md-strip__item" role="listitem" {{action "go" "boards"}}>
            <div class="md-thumb md-thumb--d md-thumb--with-icon" aria-hidden="true">
              <img src={{path "images/pastel-star.svg"}} class="md-thumb__icon" alt="">
            </div>
            <div class="md-strip__label">{{t "Board 10" key="board_10"}}</div>
          </button>
          <button type="button" class="md-strip__item" role="listitem" {{action "go" "boards"}}>
            <div class="md-thumb md-thumb--e md-thumb--with-icon" aria-hidden="true">
              <img src={{path "images/browse-boards-icon.svg"}} class="md-thumb__icon" alt="">
            </div>
            <div class="md-strip__label">{{t "Board 11" key="board_11"}}</div>
          </button>
          <button type="button" class="md-strip__item" role="listitem" {{action "go" "boards"}}>
            <div class="md-thumb md-thumb--f md-thumb--with-icon" aria-hidden="true">
              <img src={{path "images/icon-boards.png"}} class="md-thumb__icon" alt="">
            </div>
            <div class="md-strip__label">{{t "Board 12" key="board_12"}}</div>
          </button>
        </div>
        <button type="button" class="md-btn md-btn--primary md-btn--wide" {{action "goTab" "boards"}}>
          {{t "View All Boards →" key="view_all_boards"}}
        </button>
      </article>
      {{/if}}

      {{#if (is-equal this.activeTab "supervisors")}}
      <article class="md-card md-card--sup" aria-label={{t "Supervisors" key="supervisors"}}>
        <div class="md-card__head md-card__head--between">
          <div class="md-card__title">{{t "Supervisors" key="supervisors"}}</div>
        </div>
        <div class="md-people">
          <div class="md-person">
            <div class="md-avatar md-avatar--p2" aria-hidden="true"></div>
            <div class="md-person__meta">
              <div class="md-person__name">Sarah Chen</div>
              <div class="md-person__role">{{t "Supervisor" key="supervisor"}}</div>
            </div>
          </div>
          <div class="md-person">
            <div class="md-avatar md-avatar--p1" aria-hidden="true"></div>
            <div class="md-person__meta">
              <div class="md-person__name">Marcus Webb</div>
              <div class="md-person__role">{{t "Supervisor" key="supervisor"}}</div>
            </div>
          </div>
          <div class="md-person">
            <div class="md-avatar md-avatar--p3" aria-hidden="true"></div>
            <div class="md-person__meta">
              <div class="md-person__name">Elena Torres</div>
              <div class="md-person__role">{{t "Supervisor" key="supervisor"}}</div>
            </div>
          </div>
          <div class="md-person">
            <div class="md-avatar md-avatar--p4" aria-hidden="true"></div>
            <div class="md-person__meta">
              <div class="md-person__name">James Park</div>
              <div class="md-person__role">{{t "Supervisor" key="supervisor"}}</div>
            </div>
          </div>
          <div class="md-person">
            <div class="md-avatar md-avatar--p5" aria-hidden="true"></div>
            <div class="md-person__meta">
              <div class="md-person__name">Yuki Tanaka</div>
              <div class="md-person__role">{{t "Supervisor" key="supervisor"}}</div>
            </div>
          </div>
          <div class="md-person">
            <div class="md-avatar md-avatar--p6" aria-hidden="true"></div>
            <div class="md-person__meta">
              <div class="md-person__name">David Okonkwo</div>
              <div class="md-person__role">{{t "Supervisor" key="supervisor"}}</div>
            </div>
          </div>
          <div class="md-person">
            <div class="md-avatar md-avatar--p7" aria-hidden="true"></div>
            <div class="md-person__meta">
              <div class="md-person__name">Rachel Green</div>
              <div class="md-person__role">{{t "Supervisor" key="supervisor"}}</div>
            </div>
          </div>
          <div class="md-person">
            <div class="md-avatar md-avatar--p8" aria-hidden="true"></div>
            <div class="md-person__meta">
              <div class="md-person__name">Alex Rivera</div>
              <div class="md-person__role">{{t "Supervisor" key="supervisor"}}</div>
            </div>
          </div>
        </div>
      </article>
      {{/if}}

      {{#if (is-equal this.activeTab "extras")}}
      <section class="md-extras-page" aria-label={{t "Extras" key="extras"}}>
        <h2 class="md-extras-page__title">{{t "More Tools & Resources" key="more_tools"}}</h2>
        <p class="md-extras-page__sub">{{t "Games & resources" key="games_and_resources"}}</p>
        <div class="md-extras-page__grid" role="list">
          {{#each this.extrasItems as |item|}}
            {{#if item.show}}
              <article class="md-extras-card" role="listitem">
                <div class="md-extras-card__icon-wrap {{if (is-equal item.action "newBoard") "md-extras-card__icon-wrap--new-board"}} {{if (is-equal item.action "intro") "md-extras-card__icon-wrap--learn-setup"}}">
                  <img src={{path item.image}} class="md-extras-card__img" alt="" aria-hidden="true">
                </div>
                <div class="md-extras-card__body">
                  <h3 class="md-extras-card__title">{{t item.title_default key=item.title_key}}</h3>
                  <p class="md-extras-card__sub">{{t item.subtitle_default key=item.subtitle_key}}</p>
                </div>
                <button type="button" class="md-btn md-btn--primary md-extras-card__btn" {{action "extraAction" item.action}}>
                  {{t item.btn_default key=item.btn_key}}
                </button>
              </article>
            {{/if}}
          {{/each}}
        </div>
      </section>
      {{/if}}
    </section>
  </main>

  {{#if this.isSearchOpen}}
    <section class="md-searchOverlay" role="dialog" aria-label={{t "Search" key="search"}}>
      <div class="md-searchOverlay__top">
        <input
          class="md-searchOverlay__input"
          placeholder={{t "Search boards, reports, supervisors…" key="md_search_placeholder"}}
          autofocus
          {{action "onSearchKeydown" on="keyDown"}}
        />
        <button type="button" class="md-btn md-btn--ghost" {{action "closeSearch"}}>
          {{t "Close" key="close"}}
        </button>
      </div>
      <div class="md-searchOverlay__body">
        <div class="md-muted">{{t "Hook this to your Rails endpoint later." key="md_search_hook"}}</div>
        <div class="md-quickActions">
          <button type="button" class="md-row" {{action "goAndCloseSearch" "speak"}}>{{t "Continue Speaking" key="continue_speaking"}}</button>
          <button type="button" class="md-row" {{action "goAndCloseSearch" "boards"}}>{{t "Open Boards" key="open_boards"}}</button>
          <button type="button" class="md-row" {{action "goAndCloseSearch" "reports"}}>{{t "View Reports" key="view_reports"}}</button>
          <button type="button" class="md-row" {{action "openExtrasTab"}}>{{t "Explore Extras" key="explore_extras"}}</button>
        </div>
      </div>
    </section>
  {{/if}}
  {{outlet}}
</div>

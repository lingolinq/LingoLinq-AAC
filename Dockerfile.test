# Test minimal Dockerfile to isolate registry issues
FROM ruby:3.2.8

WORKDIR /app
COPY Gemfile* ./
RUN bundle install
COPY . .

EXPOSE 3000
CMD ["bundle", "exec", "puma", "-t", "5:5", "-p", "3000", "-e", "production"]
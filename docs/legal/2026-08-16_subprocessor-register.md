# LingoLinq Subprocessor Register

**Owner:** Privacy Office (privacy@lingolinq.com)
**Supersedes:** `docs/legal/SUBPROCESSORS.md`, which remains frozen at the bytes attested 2026-08-08 with its own attestation block unaltered. This dated record is the operative subprocessor register from 2026-08-16 forward. Attestation state for this record is maintained in the document register (`audit-reports/DOCUMENT-REGISTER.json`), which is authoritative; this file deliberately asserts no attestation state of its own, so that these bytes cannot go stale the moment the state changes.
**Reason for supersession:** The predecessor's row 4 (Anthropic) and section 5.7 state that the runtime AI path is "credentialled but carrying no traffic, verified 2026-08-07", cite serving revision `lingolinq-web-00017-n65`, and answer "Reaches vendor: currently no". All three were overtaken by the production deploy of PR #734 on 2026-08-12. **The data-flow determination changes: user-attributed prompts now reach Anthropic.** See section 4 row 4, section 5.7, and the 2026-08-16 change-log entry.
**Last reviewed:** 2026-08-16 (Anthropic row and section 5.7 re-verified against live production infrastructure; no other vendor row re-reviewed — the last full line-by-line review remains 2026-08-08, see section 4.1)
**Prior header line, retained for the record — Last reviewed:** 2026-08-08 (last completed CEO re-attestation; prior re-attestations 2026-08-06, 2026-08-04 and 2026-07-23; last full line-by-line review 2026-08-08, see section 4.1)
**Re-attestation status:** Maintained in `audit-reports/DOCUMENT-REGISTER.json` for this record's row, which is authoritative. The predecessor's status line, which read "COMPLETE for the current revision, attested 2026-08-08", described the predecessor's bytes and is retained there, not restated here.
**Attested:** 2026-07-06; re-attested 2026-07-23; re-attested 2026-08-04; re-attested 2026-08-06; **re-attested 2026-08-08** by Scot Wahlquist,
CEO. The 2026-07-06 attestation covered an earlier revision; five later PRs (#588, #622, #631,
#648, #652) rewrote the Gemini, Google Cloud, Anthropic, and localization/speech rows. The
2026-07-23 re-attestation is the CEO sign-off the 2026-07-21/22/23 amendments below were marked
DRAFT pending; it also covers the same-day **Abair Irish TTS disable** (#674, row #17) and the
localization/speech finding triage recorded in the change log below. The **2026-08-04**
re-attestation covers the two row-4 (Anthropic) runtime-status corrections in the change log,
dated 2026-08-02 and 2026-08-04. The **2026-08-06** re-attestation covers the record-completeness
correction recorded in the 2026-08-06 change-log entry: the two previously absent "Notified to
customers" cells, the corrected "Last amended" date, and the section 2 customer-notice-timing
clause bounded to the period it was true. No vendor, data flow, contract, or classification
changed in that revision. Those bytes were corrected before merge and never reached `staging`, so
the 2026-08-06 attestation is superseded rather than void. The **2026-08-08** re-attestation covers
the current revision, which additionally corrects the Anthropic/Bedrock runtime status against live
infrastructure, fixes the header amendment chain that the 2026-08-06 revision left naming
2026-07-23 as the prior amendment, reconciles the review cadence, and records the quarterly
full-register review performed 2026-08-08 together with the two register omissions it found (new
section 4.1). This attestation is made with those two omissions disclosed on the face of the
register, not resolved: Stripe and iplocate.io still need rows and DPA verification.

> **Discharge note, 2026-08-05.** The 2026-08-02 and 2026-08-04 change-log rows below each end
> with "Re-attestation owed." That was true when each row was written and was **discharged on
> 2026-08-04**, when Scot Wahlquist re-attested this document (PR #725). Those sentences are
> retained as written so the change log stays an accurate historical record; read them together
> with this note and with the dated discharge appended to each row. **Scope of this note, narrowed
> 2026-08-07:** it discharges the re-attestation owed by the 2026-08-02 and 2026-08-04 rows ONLY.
> It originally ended "No re-attestation of this document is outstanding," which was true on
> 2026-08-05 but was not true of the revision prepared 2026-08-07, for which a re-attestation was
> then outstanding. That one was completed 2026-08-08. This note is retained as written for the
> historical record; the Re-attestation status line above governs the current revision.

**Last amended:** 2026-08-08 (**quarterly full-register review performed**, and it found two active
third-party processors absent from the section 4 table: **Stripe, Inc.** and **iplocate.io**. Both
are disclosed in new **section 4.1** on identification rather than held until full rows are drafted,
so this table is not mistaken for a complete inventory meanwhile; drafting their rows requires DPA
verification, which is separate tracked work. The ungated `iplocate.io` call site is raised as
findings-register entry LL-07f1869d92. See the 2026-08-08 change-log entry)

**Prior amendment, 2026-08-07:** (three corrections. (1) **Runtime status of the Anthropic/Bedrock path
corrected against live infrastructure**: row 4 and the section 5.7 consistency note asserted that no
`lingolinq-web` revision carried a Bedrock credential; credentials have in fact been continuously
mounted on the serving revision since 2026-08-04T07:25:08Z. No recorded call carries learner
content and the data-flow determination is unchanged, subject to the evidentiary limits stated in
row 4 (`AiApiLog` can under-record, and vendor-side confirmation is not yet
obtainable). (2) Header revision chain: the history
below named 2026-07-23 as the prior amendment, skipping 2026-07-24, 2026-08-02 and 2026-08-04.
(3) Review cadence reconciled with the document register. See change log)

**Earlier amendments, most recent first:** 2026-08-06 (record-completeness correction: the "Notified
to customers" cells omitted from the 2026-08-02 and 2026-08-04 change-log rows completed from each
row's own section 2 determination, the "Last amended" date corrected, and the section 2
customer-notice-timing clause bounded to the period it was true); 2026-08-04 and 2026-08-02 (row 4
Anthropic runtime-status corrections); 2026-07-24 (section 2 subprocessor-notice position recorded
for the GCP infrastructure addition #19). Each is recorded in full in the change log below.

**Oldest amendment narrated in this header, 2026-07-23:** (**Abair Irish TTS (#17) DISABLED** via PR #674 -- no DPA on file and Google Cloud TTS has no Irish voice, so Irish cloud TTS was turned off rather than executing a DPA; the localization/speech findings were triaged: LL-a167848115 remediated; LL-c38e7da48e (#15) and LL-1eb9a2435b (#18) accepted-risk, with a per-flow COPPA-consent gate the one remaining open follow-up (deferred to before real tenant onboarding). Earlier the same day, PR #667 remediated the Cloud TTS Pre-GA `v1beta1` endpoint residual: all six call sites repointed to GA `v1` and the unauthenticated consumer `translate_tts` fallback removed. Prior 2026-07-22 amendments: (1) Gate 1 GCP cutover recorded -- GCP infrastructure host added as #19; (2) localization/speech re-scope + covered-service confirmation -- added Google Cloud Speech-to-Text (#18) and confirmed all three Google localization/speech services are BAA covered products, independently verified against Google's HIPAA covered-products list. CEO sign-off recorded 2026-07-23; at that date, Privacy Office confirmation of the customer-notice timing under section 2 was recorded as still owed before external publication. **[Answered 2026-07-24.** The change-log entry of that date records the CEO position that no section 2 notice is owed, and adds section 2's **Pre-tenant baseline** paragraph. That paragraph governs notice timing for any subprocessor change made while production carries only synthetic/test data with no real tenant onboarded, which is the condition every 2026-07-21/22/23 amendment was made under. This sentence is retained as the historical record of the 2026-07-23 state; nothing is outstanding.**]** See change log)
**Last full review:** 2026-08-08 (quarterly full-register review performed; findings in section 4.1
and the 2026-08-08 change-log entry. Prior full line-by-line review 2026-04-20)
**Next review:** 2026-11-08 (quarterly; cadence confirmed by the CEO 2026-08-07. All three
scheduling sources agree: this line, the document register's `nextReviewDue`, and the compliance
calendar item `rev-gdpr-subprocessor-quarterly`. **Missed cycle on record:** on a quarterly cadence
from 2026-04-20 a cycle fell due 2026-07-20 and was not performed. Rather than re-date the schedule
past that gap, which would have hidden an overdue Article 28 review, the CEO directed on 2026-08-08
that the review be performed; it was, and it found two undisclosed processors. The dated amendments
in the change log below are per-change updates, not the periodic full-register review this cadence
tracks)
**Related:** `docs/legal/BREACH_RUNBOOK.md`, `docs/legal/DATA_RETENTION.md`, `COMPLIANCE.md`

## 1. Purpose

This register lists every third party that LingoLinq uses to process data in support of the LingoLinq platform. It is maintained to satisfy Article 28 of GDPR, the HIPAA Business Associate obligations at 45 CFR § 164.502(e), and the transparency expectations of US school and hospital customers under FERPA and state student-data-privacy laws.

## 2. Change Notification

LingoLinq will provide customers with at least 30 days advance notice before any change to this subprocessor list that affects their tenant. Customers may object to a proposed change by writing to privacy@lingolinq.com. Notifications are delivered by email to the named privacy contact for each tenant and posted at https://lingolinq.com/legal/subprocessors.

**Pre-tenant baseline.** This obligation runs to existing tenants: it is an *advance* notice of a *change* that affects a tenant's data. A subprocessor added while production carries only synthetic/test data and no real tenant has been onboarded (see §5.7) affects no tenant, so it triggers no advance notice. Such additions form the disclosed subprocessor baseline presented to the first real tenant at onboarding. The 30-day advance-notice clock applies to any subprocessor change made *after* a real tenant is live. Confirming the published list is current before the first real-tenant onboarding is tracked in the compliance calendar (`gate-first-tenant-subprocessor-baseline`).

## 3. Legend

- **Student or patient data reaches this vendor?** "Yes" means the vendor can receive identifying or content data tied to a learner or patient — including pseudonymized learner content (scrubbed of direct identifiers by design), which remains personal data under GDPR/UK-GDPR. "No" means the vendor only receives aggregate or LingoLinq corporate data.
- **DPA / BAA status:** "Signed [date]" is executed and on file. "Via standard terms" means the vendor's standard online DPA applies and no custom agreement is negotiated. "Pending" means in progress.

## 4. Active Subprocessors

| # | Subprocessor | Service provided | Data categories | Student or patient data reaches this vendor? | Region | DPA / BAA status | Privacy / security reference |
|---|---|---|---|---|---|---|---|
| 1 | Amazon Web Services, Inc. | S3 object storage, KMS, RDS (if used), CloudFront | User-uploaded media, database backups, application secrets | Yes | us-east-1 | BAA signed 2026-02-07, account 2390-4478-5114 | https://aws.amazon.com/compliance/data-privacy/ |
| 2 | Render Services, Inc. | Superseded primary application host; retained temporarily as write-frozen rollback fallback pending decommission | Frozen production database copy, logs, environment, and fallback app services until deleted or restricted | Yes until decommission | us-east (Ohio primary) | DPA signed; BAA pending; no new hospital tenants relying on Render fallback | https://render.com/legal/dpa and https://render.com/security |
| 3 | OpenAI, L.L.C. | GPT models (contracted); NOTE: no active code path sends data to OpenAI as of 2026-07-06 — the in-repo OpenAI SDK clients target Google's Gemini OpenAI-compatible endpoint. Row retained pending Privacy Office review (removal per section 7). | Pseudonymized (scrubbed) prompts only IF used, redacted by lib/pii_scrubber.rb before transmission; still personal data under GDPR/UK-GDPR | Currently none (no active flow); would be Yes — pseudonymized if activated | US | DPA via OpenAI API standard terms | https://openai.com/policies/data-processing-addendum |
| 4 | Anthropic, PBC | Claude models for AI board generation and word prediction (eval-narrative drafting remains unbuilt); **ACTIVE AND CARRYING USER-ATTRIBUTED TRAFFIC, verified live 2026-08-16.** The serving revision `lingolinq-web-00020-per` (100% of traffic) mounts both `BEDROCK_AWS_KEY` and `BEDROCK_AWS_SECRET` from Secret Manager. Production `AiApiLog` holds **64 rows**, of which **63 were written since the 2026-08-12 production deploy of PR #734** and **63 carry a `user_global_id`**. All 64 succeeded, all on inference profile `us.anthropic.claude-haiku-4-5-20251001-v1:0` via provider `claude`; by type, 57 `word_prediction` and 7 `board_generation`; 17,221 prompt tokens sent in total. The 63 user-attributed calls come from **2 distinct user accounts** out of 34 accounts and 2 organizations then present in production, which is consistent with the internal pre-tenant testing described in section 5.7 rather than with tenant traffic; note that this register reads that from `AiApiLog` attribution alone and does not independently certify that those two accounts are non-real. `article_50_disclosure_shown` is true on all 63 post-deploy rows and false only on the single 2026-08-04 internal verification call. `pii_detected` is false on all 64 rows and `ip_address` is null on all 64. `jurisdiction` is **null on all 64 rows**, so no row is stamped for the EU retention purge; that gap is pre-existing and tracked separately, and is recorded here because this register is where the egress is disclosed. **This supersedes the predecessor's "credentialled but carrying no traffic" and "no call is recorded" statements, and the serving revision it named (`lingolinq-web-00017-n65`).** **Evidentiary limits, stated rather than glossed and unchanged from the predecessor:** `AiApiLog` is not a durable ledger of egress. `AiApiLog.log_ai_call` rescues `ActiveRecord::ActiveRecordError` and returns an unsaved record, so a call can complete without leaving a row (`app/models/ai_api_log.rb`), and `Flusher.flush_user_logs` destroys rows by `user_global_id` when a user is erased (`lib/flusher.rb`). The counts above are therefore a **floor, not a ceiling** — which is the direction that matters now that the answer is "yes". Vendor-side confirmation (CloudWatch `AWS/Bedrock` `Invocations`, or CloudTrail `bedrock:InvokeModel`) remains **not obtained**: the available IAM principal `lingolinq-app` is denied `cloudwatch:GetMetricStatistics`. Earlier history retained: first credentialled 2026-08-03T08:23Z (`00013-76w`), withdrawn 2026-08-04T06:31:46Z (`00014-5rw`), re-mounted 53 minutes later (`00015-9l9`), continuously mounted since 2026-08-04T07:25:08Z. The prior direct `api.anthropic.com` route was removed by PR #681. A dormant `ANTHROPIC_API_KEY` secret is still mounted on the serving revision and carries no traffic; its removal is tracked in PR #805. | Pseudonymized (scrubbed) prompts, redacted by `lib/pii_scrubber.rb` before transmission; still personal data under GDPR/UK-GDPR | **Yes, as of the 2026-08-12 deploy.** 63 user-attributed calls are recorded. This answer is changed from the predecessor's "currently no", and the change is a change of runtime status rather than a subprocessor addition — Anthropic has been a listed subprocessor throughout, under a HIPAA-Ready BAA executed 2026-07-18 — so section 2's 30-day advance-notice clock is not triggered by it. Section 2's **Pre-tenant baseline** applies independently: production had no real tenant onboarded (section 5.7), so there was no tenant to notify. **Both determinations must be revisited before the first real tenant is onboarded**, because from that point this is a live learner-content flow to a third-country processor. | US | **HIPAA-Ready BAA executed + HIPAA readiness enabled 2026-07-18** (runtime-dedicated LingoLinq, LLC API org; see docs/legal/ANTHROPIC_BAA_ACCEPTED.md); supersedes the prior Commercial-Terms-DPA-only basis | https://www.anthropic.com/legal/commercial-terms |
| 5 | Google LLC (Gemini API) | Gemini models for AI board generation and word prediction; NOTE: runtime path DISABLED 2026-07-09 -- the GEMINI_API_KEY fallback was removed from lib/ai_word_predictor.rb, lib/ai_prediction_generator.rb, and lib/ai_board_generator.rb (Gemini Developer/AI-Studio endpoint data-handling terms inadequate for child data). No active code path sends data to Gemini today; a Vertex AI fallback may replace it. Row retained pending reactivation (compliance calendar rev-gemini-baa-annual; removal per section 7). | Pseudonymized (scrubbed) prompts only IF used, redacted by lib/pii_scrubber.rb before transmission; still personal data under GDPR/UK-GDPR | Currently none (runtime path disabled 2026-07-09); would be Yes -- pseudonymized if reactivated | US | DPA via Google Cloud Terms of Service and Data Processing Addendum | https://cloud.google.com/terms/data-processing-addendum |
| 6 | HubSpot, Inc. | Marketing CRM, lifecycle email, customer support | Prospect names, emails, company names, marketing engagement events; no student data | No (LingoLinq customer records only) | US, with EU regional options | DPA via HubSpot Customer DPA | https://legal.hubspot.com/dpa |
| 7 | Functional Software, Inc. (Sentry) | Application error monitoring | Stack traces, request metadata, optional user ID; PII scrubbing filters active | Potentially yes if filters fail; treat as Yes for review | US | DPA via Sentry Customer DPA | https://sentry.io/trust/ |
| 8 | n8n GmbH (self-hosted on LingoLinq Render) | Internal workflow automation | No customer data; operational signals only | No | us-east (same Render infrastructure) | No third-party processing; covered by Render | https://n8n.io/legal/ |
| 9 | Cake.com Inc. (Clockify) | Internal time tracking | LingoLinq employee and contractor time entries; no customer data | No | US | DPA via Clockify standard terms | https://clockify.me/privacy-policy |
| 10 | Render Managed PostgreSQL | Superseded production database retained as write-frozen rollback fallback pending decommission | Frozen production data copy until deleted or restricted | Yes until decommission | us-east | Covered by Render DPA, BAA pending | https://render.com/docs/databases |
| 11 | Cloudflare, Inc. | DNS resolution; CDN in front of marketing site where applicable | Request metadata, IP addresses, user agents for traffic to public endpoints | Incidental only (IP and UA for public traffic) | Global anycast | DPA via Cloudflare Customer DPA | https://www.cloudflare.com/cloudflare-customer-dpa/ |
| 12 | Google LLC (Google Workspace) | LingoLinq corporate email, calendar, Drive, Chat | LingoLinq employee and contractor business data | No (corporate productivity only) | US, EU failover | DPA via Google Workspace DPA | https://workspace.google.com/terms/dpa_terms.html |
| 13 | 1Password Corp. | Password and secrets management for LingoLinq staff | LingoLinq internal secrets; no customer data | No | US and Canada | DPA via 1Password standard terms | https://1password.com/legal/data-processing-agreement |
| 14 | GitHub, Inc. | Source code hosting and CI | LingoLinq source code, issue content; customer data is not permitted in this system | No (policy: no customer data) | US | DPA via GitHub Customer DPA | https://docs.github.com/en/site-policy/privacy-policies/github-data-protection-agreement |
| 15 | Google LLC (Cloud Translation API) | Machine translation of board button labels and word entries (Cloud Translation API v2). Code: `app/models/word_data.rb#query_translations`, reached from `Board#translate_set`, `boards_controller#translate`, `users_controller#translate`, `lib/spanish_library_boards.rb`. | **Raw, UNSCRUBBED user-authored button-label / word text** -- does NOT pass through `lib/pii_scrubber.rb`; a personalized label can carry a child's, family member's, teacher's, or school name. See §5.8. | Yes -- raw user content (prod currently synthetic/test only, pre-MVP; flow is live, gated on `GOOGLE_TRANSLATE_TOKEN`, which is set) | US (googleapis.com) | Covered by the Google Cloud CDPA + entity BAA recorded in §5.7 (accepted 2026-07-12/14 for `lingolinq-prod`; org-wide 2026-06-08). HIPAA covered-service **CONFIRMED 2026-07-22** (Google HIPAA covered-products list, independently verified). Endpoint is Translation API **v2 (GA)** -- no Pre-GA caveat. | https://cloud.google.com/terms/data-processing-addendum |
| 16 | Google LLC (Cloud Text-to-Speech) | Speech-audio generation for spoken text and board auto-sound. Code: `lib/tts.rb#generate_google`, `app/controllers/api/search_controller.rb`. | **Raw, UNSCRUBBED text being spoken** (AAC utterance content) -- not scrubbed; can contain typed identifiers. See §5.8. | Yes -- raw user content (prod currently synthetic/test only, pre-MVP; flow is live) | US (googleapis.com) | Same Google Cloud CDPA + entity BAA as #15 (§5.7). Product covered-service **CONFIRMED 2026-07-22** (Google HIPAA covered-products list). Pre-GA endpoint residual **REMEDIATED 2026-07-23 (PR #667)**: all call sites repointed from the Pre-GA `v1beta1` endpoint to GA `v1` (`lib/tts.rb`, `app/controllers/api/search_controller.rb`), and the unauthenticated consumer `translate_tts` fallback was removed. TTS now rides a GA covered-service endpoint, within BAA coverage for PHI. Tracked in LL-a167848115. | https://cloud.google.com/terms/data-processing-addendum |
| 17 | Trinity College Dublin -- ADAPT Centre (abair.ie) | Irish-language (Gaeilge) text-to-speech synthesis. **DISABLED 2026-07-23** (PR #674) -- the Irish-TTS code paths (`lib/tts.rb`, `app/controllers/api/search_controller.rb#audio`) now short-circuit for `ga` locales and make no request to `https://abair.ie/aac_irish`. | None -- **no data flows to this endpoint** while disabled. | **No -- disabled** (no request is made) | Ireland (EU) | **DISABLED 2026-07-23 per CEO decision** (PR #674) rather than executing a DPA (finding LL-a167848115). Had **no DPA on file**; it is the only EU-hosted flow. Google Cloud TTS has no Irish voice, so there is no covered replacement. Re-enabling Irish TTS requires a DPA/SCCs first. | https://abair.ie |
| 18 | Google LLC (Cloud Speech-to-Text) | Transcription of user-recorded button-sound audio (Speech-to-Text v1 `speech:recognize`). Code: `app/models/button_sound.rb#schedule_transcription`, fired automatically by the `after_save :schedule_transcription` hook whenever a recorded sound has a `secondary_url` and no transcription yet. Uses `GOOGLE_TRANSLATE_TOKEN` (set). | **Raw user VOICE RECORDING (audio blob), the highest-sensitivity flow in this register** -- an actual recording of a communicator or caregiver, uploaded whole. Audio **cannot** be run through `lib/pii_scrubber.rb` (the scrubber only redacts text); voice is biometric-adjacent and may be GDPR Art. 9 special-category data. See §5.8. | Yes -- raw user voice audio (prod currently synthetic/test only, pre-MVP; flow is live and automatic) | US (googleapis.com) | Same Google Cloud CDPA + entity BAA as #15/#16 (§5.7). Covered-service **CONFIRMED 2026-07-22** (Google HIPAA covered-products list). Endpoint is Speech-to-Text **v1 (GA)** -- no Pre-GA caveat. | https://cloud.google.com/terms/data-processing-addendum |
| 19 | Google LLC (Google Cloud Platform infrastructure) | Live production application hosting on Cloud Run, Cloud SQL PostgreSQL, Memorystore Redis, load balancing, and supporting logs for project `lingolinq-prod` | Full tenant application data in compute and database; operational logs; secrets surfaced through GCP Secret Manager | Yes | US | CDPA + HIPAA BAA accepted 2026-07-12; SCCs certified 2026-07-14 | https://cloud.google.com/terms/data-processing-addendum and `docs/legal/GCP_BAA_ACCEPTED.md` |

### 4.1 Known gaps in this table, identified by the 2026-08-08 quarterly review

**This register is not currently complete, and says so rather than implying otherwise.** The
quarterly review performed 2026-08-08 (the cycle that fell due 2026-07-20 and was not performed)
reconciled the table above against every external host referenced in `lib/`, `app/models/`,
`app/controllers/` and `config/`. It identified **two active third-party processors that this table
does not list**. Both are recorded here on identification rather than held back until full rows are
drafted, so that no reader mistakes the table for a complete inventory in the interim.

| Processor | What it receives | Code path | Status |
|---|---|---|---|
| **Stripe, Inc.** | Payment and customer-identity data for paid subscriptions | `lib/purchasing.rb` (`Stripe.api_key`, `Stripe::Customer.retrieve`, `Stripe::Charge.retrieve`); `gem 'stripe'` | **Identified, row not yet drafted.** DPA status not yet verified. Named nowhere in this register before 2026-08-08. |
| **iplocate.io** | A user's **IP address**, for geolocation | `lib/external_tracker.rb` (gated on `external_email_allowed?` + `supporter_registration?` + `!cookies_opted_out?`) and `app/mailers/subscription_mailer.rb#new_subscription` (**no user-type or consent gate**) | **Identified, row not yet drafted.** DPA status not yet verified. The ungated call site is tracked as finding **LL-07f1869d92**. |

Neither omission is new behaviour: both code paths predate this review. They are recorded as
register defects, of the same class as the three localization/speech flows recorded 2026-07-21 and
the Speech-to-Text flow recorded 2026-07-22. Drafting full rows requires DPA verification for each
vendor, which is tracked separately and is not resolved by this entry. Until those rows exist,
**read section 4 together with this subsection.**

## 5. Data Flow Notes

### 5.1 AI API calls (OpenAI, Anthropic, Google)

All runtime AI API calls that involve user content pass through `lib/pii_scrubber.rb` before transmission. The scrubber applies a pattern, key, and blocklist strategy to redact names, emails, phone numbers, and tenant-identifying tokens. Redacted payloads are logged to `AiApiLog` for audit. Feature flags in `lib/feature_flags.rb` gate which tenants have AI features enabled. (The offline `AiPredictionGenerator` dictionary builder also calls AI APIs, but sends only generic starter words from built-in word lists — no user or tenant content — so it is neither scrubbed nor logged to `AiApiLog`.)

Scrubbed prompts are classified as **pseudonymized personal data, not anonymous or de-identified data**: the scrubber removes direct identifiers, but the payloads remain personal data under GDPR/UK-GDPR and are treated as such in this register. Pseudonymization here is a safeguard (Article 32), not an exemption from data protection obligations.

Scrubbing removes *known* direct identifiers — patterns (emails, phones), account-derived names, and configured blocklists. A free-hand third-party name (family member, peer, school staff) typed into free text that matches none of those sources can evade the scrubber; NER-based coverage remains an open item (see the limitation note in `lib/eval_narrator.rb`). Scrubbing is therefore a strong safeguard, designed to remove direct identifiers, not an absolute guarantee — which is why the AI vendor rows above answer "Yes — pseudonymized" rather than "No".

### 5.2 Render

Render was the production hosting platform before the 2026-07-22 Gate 1 DNS cutover. It is now
superseded as the active branded-domain host by Google Cloud Platform, but remains online as a
write-frozen rollback fallback at `https://lingolinq-prod.onrender.com` until a separately
authorized decommission. Managed PostgreSQL and managed Redis in Render therefore remain in this
register until deletion or restriction is verified. BAA negotiation with Render is no longer the
hosting path for new production traffic, and LingoLinq does not onboard new hospital tenants that
would rely on the Render fallback.

### 5.3 AWS

AWS is used for S3 object storage (board media, recordings, backups) and KMS key management. The AWS BAA covers all HIPAA-eligible services used by LingoLinq and was executed on 2026-02-07 for account 2390-4478-5114.

### 5.4 Sentry PII posture

Sentry SDK configuration in the Rails application must enable `send_default_pii: false` and run the project-level data scrubbing rules. Any incident where Sentry ingests raw PII is treated as a subprocessor breach and handled per `BREACH_RUNBOOK.md`. Configuration is reviewed quarterly.

### 5.5 HubSpot

HubSpot receives data via `lib/external_tracker.rb`. The code path gates on `supporter_registration?`, `external_email_allowed?`, and `cookies_opted_out?`. Student and patient accounts never reach HubSpot; only marketing-qualified supporters and paying customer contacts do. COPPA review of the registration flow is in progress.

### 5.6 AWS SNS (SMS delivery)

The `lib/pusher.rb` module is an internal naming relic from the CoughDrop fork; it is an `aws-sdk-sns` wrapper used to deliver transactional SMS (supervisor consent invitations, two-factor codes, password resets). Phone numbers and short message bodies are transmitted to AWS SNS; no communication content or board data is sent. This flow is covered by the AWS BAA executed on 2026-02-07 and is listed under subprocessor #1 (Amazon Web Services).

### 5.7 Google Cloud Platform / Cloud Run (active production infrastructure)

LingoLinq completed the Gate 1 DNS cutover on 2026-07-22. `app.lingolinq.com` now serves from
Google Cloud Platform, with production compute on Cloud Run, relational data on Cloud SQL
PostgreSQL, and Redis/Resque on Memorystore over TLS. Object storage and email remain on AWS.
Google Cloud Platform infrastructure is now listed as active subprocessor #19.

The Google Cloud data-processing and HIPAA terms for project `lingolinq-prod` were reviewed and
accepted in the GCP console by scot@lingolinq.com: the Cloud Data Processing Addendum (CDPA) and the
Google Cloud HIPAA Business Associate Addendum (BAA) on 2026-07-12, and the European Data Protection
Law Standard Contractual Clauses (EU GDPR, UK GDPR, Swiss FDPA) certified 2026-07-14. This
formalizes at the `lingolinq-prod` project level the org-wide Google BAA previously accepted
2026-06-08. Under the HIPAA BAA, PHI is permitted on Google Cloud subject to BAA terms, which are
necessary but not sufficient (HIPAA-eligible services only, encryption in transit and at rest, access
controls, minimum necessary). Google issues no countersigned PDF for these; the console acceptance
record is the authoritative evidence and is captured in-repo at `docs/legal/GCP_BAA_ACCEPTED.md`.
Evidence: Google Drive "Compliance Audits" folder, "Google Cloud Platform - Accepted Compliance
Agreements (captured 2026-07-14)" plus the source console screenshots; Notion "Vendor BAAs &
Subprocessor Registry" Google row updated 2026-07-14 and cutover status updated 2026-07-22.

Scope boundary: this is an **infrastructure** BAA covering the products on Google's HIPAA Covered
Products list in use for hosting (Cloud Run, Cloud SQL, Memorystore); it does not extend to Vertex
AI as a whole, so any future Vertex AI or Gemini inference path requires per-product covered-service
verification before PHI or child data. It does **not** cover the Anthropic model-provider egress
path, which is covered by Anthropic's own HIPAA-Ready BAA (executed 2026-07-18; see
`ANTHROPIC_BAA_ACCEPTED.md`) with the PiiScrubber and no-identifiable-data policy retained as
defense-in-depth. The compliance-calendar Gemini-path item (`rev-gemini-baa-annual`) is likewise a
separate model-provider concern.

Consistency note (2026-07-12 Two-Plane AI Architecture decision): that decision's target of production inference via Vertex AI would first require confirming that Vertex AI (or the specific Vertex/Gemini product used) is a Google HIPAA Covered Service; the infrastructure BAA does not by itself extend to it. It is a direction, not a live path. The designated runtime model path is Anthropic Claude on AWS Bedrock. **Status re-verified live 2026-08-16: ACTIVE and carrying user-attributed traffic.** The serving revision `lingolinq-web-00020-per` mounts both Bedrock secrets, and production `AiApiLog` holds 64 rows of which 63 were written since the 2026-08-12 production deploy of PR #734 and 63 carry a `user_global_id`. **This supersedes the predecessor's "credentialled, carrying no traffic" status and its citation of revision `lingolinq-web-00017-n65`.** The audit table can under-record (rescued persist failures; per-user erasure by `Flusher`) and vendor-side confirmation has not been obtained, so the counts are a floor rather than a guarantee; see the row 4 note in section 4. The Google Gemini/Vertex inference fallback was disabled 2026-07-09 (PR #570), so no AI inference reaches Google today. **Correction history retained:** the predecessor corrected this note on 2026-08-07 (it had claimed the path was dormant because no revision carried a Bedrock credential, a premise that stopped being true on 2026-08-04T07:25:08Z), and on 2026-08-04 before that. This is the third correction to the same cell in nine days; see the 2026-08-16 change-log entry for why the recurrence is itself recorded. `docs/legal/AWS_BAA_ACCEPTED.md` carries the same superseded "no traffic" claim and is corrected separately.

Google LLC also appears in the table for the **Gemini API** (#5, disabled runtime path) and
**Google Workspace** (#12, corporate productivity). The Cloud Run / infrastructure relationship is
distinct from both and is listed separately as row #19.

### 5.8 Localization and speech flows are NOT scrubbed (triaged 2026-07-23)

Three runtime flows send **raw, unscrubbed user content** to a third party and, unlike the AI rows in §5.1, do **not** pass through `lib/pii_scrubber.rb` (a fourth, Abair Irish TTS, was **DISABLED 2026-07-23** via PR #674 and no longer sends any data -- see below):

- **Cloud Translation** (#15) -- board button labels / word strings, `app/models/word_data.rb#query_translations`.
- **Cloud Text-to-Speech** (#16) -- spoken text, `lib/tts.rb#generate_google` and `app/controllers/api/search_controller.rb`.
- **Cloud Speech-to-Text** (#18) -- a **user voice recording (audio blob)**, `app/models/button_sound.rb#schedule_transcription`, fired automatically by an `after_save` hook. This is the highest-sensitivity flow here and the one added in the 2026-07-22 re-scope.

**Why "scrub it" is the wrong frame for most of these.** Text scrubbing is deliberately absent because redacting a label or utterance corrupts the very output the feature produces (you cannot translate or speak a name you have removed), and for the audio flow (#18) scrubbing is not even possible -- `pii_scrubber.rb` operates on text, not on a voice recording. The correct primary control for the Google flows (#15/#16/#18) is therefore **not** a scrubber but (a) each being a **covered service under the existing Google Cloud BAA** recorded in §5.7, plus (b) disclosure (this register). **Covered-service status was CONFIRMED 2026-07-22** against Google's canonical HIPAA covered-products list (cloud.google.com/security/compliance/hipaa, independently verified): Cloud Translation, Speech-to-Text, and Text-to-Speech are all listed. Where the BAA covers the service, raw content is contractually permitted the same way it is for any covered Cloud processor.

**The Pre-GA endpoint residual is now REMEDIATED (2026-07-23, PR #667).** Google's BAA **excludes Pre-GA offerings from PHI** ("Do not use Pre-GA offerings ... in connection with PHI"). Cloud Translation (v2) and Speech-to-Text (v1) always called GA endpoints. Cloud Text-to-Speech previously called the Pre-GA `v1beta1` endpoint at all six call sites; PR #667 repointed every one to the GA `v1` endpoint (`lib/tts.rb`, `app/controllers/api/search_controller.rb`), so all three Google flows now ride GA covered-service endpoints and are within BAA coverage for PHI. **The cluster was TRIAGED by the CEO 2026-07-23** (findings **LL-c38e7da48e** translation, **LL-a167848115** TTS: Google TTS + Abair, **LL-1eb9a2435b** voice-audio transcription #18):

- **LL-a167848115 (TTS) -- remediated.** Every named sub-defect is closed: Abair disabled (PR #674), Google TTS repointed to the GA `v1` endpoint (#667), and the consumer `translate_tts` fallback removed (#667). The Google TTS flow that remains rides a GA covered-service endpoint under the BAA.
- **LL-c38e7da48e (translation #15) and LL-1eb9a2435b (voice audio #18) -- accepted risk.** Both are covered services under the Google Cloud BAA on GA endpoints and are now disclosed here. The one residual -- raw user content auto-egresses without a per-flow COPPA-consent gate -- is **consciously accepted for the pre-MVP window** because (a) the BAA covers the processing and (b) `lingolinq-prod` carries only synthetic/test data (§5.7), so no real learner content transits these flows yet. **To revisit before real tenant onboarding:** a per-flow consent gate and/or a feature flag to disable a flow per tenant.

Until the consent posture is revisited, rows #15/#18 answer "Yes -- raw" rather than "Yes -- pseudonymized".

Two flows had **no contractual basis at all**. Both are now resolved by removal/disable:

- ~~`app/controllers/api/search_controller.rb` consumer `translate.google.com/translate_tts` fallback~~ -- **REMOVED 2026-07-23 (PR #667).** This unauthenticated consumer endpoint (no DPA basis) was deleted; the audio endpoint now returns `400 "no tts provider configured"` when no contracted provider is set, instead of posting user text to the consumer endpoint. Was part of finding LL-a167848115.
- ~~**Abair** (#17) Irish TTS~~ -- **DISABLED 2026-07-23 (PR #674).** Abair had no DPA on file and was the only EU-hosted flow. Rather than execute a DPA/SCCs, the CEO chose to disable Irish TTS: the `ga`-locale code paths in `lib/tts.rb` and `app/controllers/api/search_controller.rb#audio` now short-circuit and make no request to `abair.ie`. Google Cloud TTS has no Irish voice, so there is no covered replacement; re-enabling requires a DPA/SCCs first. Closed the Abair half of finding LL-a167848115.

## 6. Pseudonymized (Scrubbed) Data Standard

For any subprocessor marked as receiving only pseudonymized data, LingoLinq applies the scrubbing pipeline documented in `lib/pii_scrubber.rb`. Scrubbed payloads must not include names, email addresses, phone numbers, street addresses, precise geolocation, tenant names, or any free-text field that has not been passed through the scrubber. A quarterly sampling audit verifies scrubber coverage.

Scrubbed payloads remain **personal data** under GDPR/UK-GDPR (pseudonymized, per Article 4(5)): the removal of direct identifiers reduces risk but does not render the data anonymous, and all processor obligations continue to apply to vendors receiving it. This register previously described these flows as "de-identified"; that language was corrected on 2026-07-06 (classification only — the data flows themselves are unchanged).

## 7. Exit Criteria

When LingoLinq ends a subprocessor relationship, the Privacy Contact confirms that:

- All LingoLinq data has been deleted or returned per the DPA
- Written confirmation of deletion has been obtained
- The vendor is removed from the register with a dated note in the change log below
- Customers are notified in the next subprocessor change bulletin

## 8. Change Log

| Date | Change | Notified to customers |
|---|---|---|
| 2026-04-20 | Register established, 14 subprocessors recorded | Bulletin planned for 2026-05-20 |
| 2026-04-23 | Removed Pusher Ltd. entry: `lib/pusher.rb` is an AWS SNS SMS wrapper, not a Pusher.com integration. SMS flow is now described under AWS (subprocessor #1). | Bulletin planned for 2026-05-20 |
| 2026-06-18 | Added section 5.7 flagging the planned GCP / Cloud Run migration. Google compute is NOT yet an active subprocessor; the row is added at cutover once the infrastructure BAA is filed. No change to the active list. | No customer notice yet (no active change) |
| 2026-07-06 | Reclassified AI vendor rows 3-5 (OpenAI, Anthropic, Google Gemini) from "de-identified" to "pseudonymized (scrubbed) personal data", and corrected the reaches-vendor column from "No" to "Yes — pseudonymized" for the active AI flows (pseudonymized learner content remains student personal data under GDPR/UK-GDPR). Row 3 annotated: no active code path sends data to OpenAI as of this date (in-repo OpenAI SDK clients target Gemini's endpoint); removal per section 7 pending Privacy Office review. Section 6 renamed and scrubber-limitation language qualified ("removes known direct identifiers by design", not a guarantee). Classification correction only; no change to actual data flows or vendor contracts. | Privacy Office to confirm at the 2026-07-20 quarterly review whether a clarification bulletin is warranted (no vendor or flow change) |
| 2026-07-12 | Reclassified AI vendor row 5 (Google Gemini API) from an active AI flow to DISABLED / historical, correcting the register to match runtime state. The runtime Gemini fallback was removed on 2026-07-09 (lib/ai_word_predictor.rb, lib/ai_prediction_generator.rb, lib/ai_board_generator.rb; the Gemini Developer/AI-Studio endpoint's data-handling terms are inadequate for child data). Row 5 reaches-vendor changed from "Yes -- pseudonymized" to "Currently none (no active flow)"; row retained pending reactivation (rev-gemini-baa-annual). This removes a stale active-flow claim; no new data flow is introduced. | Privacy Office to note at the 2026-07-20 quarterly review; no customer notice (removal of a flow, not a new one) |
| 2026-07-16 | Recorded execution of the Google Cloud **infrastructure** agreements for project `lingolinq-prod`: CDPA + HIPAA BAA accepted 2026-07-12 and SCCs certified 2026-07-14, formalizing the org-wide Google BAA of 2026-06-08 (section 5.7 blocker 1 marked satisfied; PHI now permitted on GCP under BAA terms). Google compute remains a **planned**, not active, subprocessor until the Render-to-GCP cutover carries production data (blocker 2 still open), so no change to the active list in section 4. Does not extend to the Anthropic AI-egress path. | No customer notice yet (no active change; the 30-day notice is due at cutover per section 2) |
| 2026-07-21 | Recorded three previously-omitted **active** data flows that send raw, unscrubbed user-authored content to third parties: Google Cloud Translation (#15), Google Cloud Text-to-Speech (#16), and Trinity College Dublin / ADAPT `abair.ie` Irish TTS (#17). These correct an omission -- the flows exist in code today (see §5.8) -- and are distinct from the AI (scrubbed) rows and the planned GCP infrastructure row. Abair has **no DPA on file** (status Pending). CEO sign-off recorded 2026-07-23; at the time of this entry the covered-service status of Cloud Translation/TTS under the Google BAA, and the egress-scrubbing decision, remain open (findings LL-c38e7da48e, LL-a167848115). | No customer notice (prod carries only synthetic/test data pre-MVP per §5.7; 30-day notice under §2 is due before real tenant data flows) |
| 2026-07-22 | Covered-service confirmation. Verified all three Google flows (#15 Translation, #16 TTS, #18 Speech-to-Text) are **BAA covered products** against Google's canonical HIPAA covered-products list (cloud.google.com/security/compliance/hipaa), independently confirmed. Recorded one concrete residual: Google's BAA excludes Pre-GA offerings from PHI, and all six Cloud **TTS** call sites use the Pre-GA `v1beta1` endpoint (Translation v2 and STT v1 are GA and clear). Remediation is a drop-in repoint to GA `v1` (shape-compatible, verified), tracked in LL-a167848115. Rows #15/#16/#18 and §5.8 updated. | No customer notice (documentation/verification only; no data-flow or contract change) |
| 2026-07-22 | Re-scope after a full egress-surface trace while designing the localization/speech control. Added Google Cloud Speech-to-Text (#18): `app/models/button_sound.rb#schedule_transcription` uploads a **user voice-recording audio blob** to `speech.googleapis.com/v1/speech:recognize` automatically via an `after_save` hook -- the highest-sensitivity flow in this register and one the 2026-07-21 pass missed entirely. Rewrote §5.8 to correct the control framing: for the Google flows the primary control is **BAA covered-service confirmation + disclosure**, not text scrubbing (which cannot apply to audio at all and would corrupt translation/TTS output); the two no-contract flows (consumer `translate_tts`, effectively dead in prod; and DPA-less Abair) are the clear-cut removals. Opened a distinct finding for #18. (Was DRAFT pending sign-off; CEO sign-off recorded 2026-07-23.) | No customer notice (prod carries only synthetic/test data pre-MVP per §5.7; 30-day notice under §2 is due before real tenant data flows) |
| 2026-07-18 | Row 4 (Anthropic) contract basis updated from Commercial-Terms DPA to an executed **HIPAA-Ready BAA with HIPAA readiness enabled** on the runtime-dedicated LingoLinq, LLC API org (verified live: Messages API 200, Files API 400; see docs/legal/ANTHROPIC_BAA_ACCEPTED.md). This is a contract-basis upgrade for an existing flow, not a new flow or a new vendor; the pseudonymized-personal-data classification and reaches-vendor="Yes" are unchanged. PHI is now permitted to Anthropic over the Messages API under BAA terms. All four runtime seams are in-scope; eval narration is classified as an assistive-technology access assessment, not a Healthcare Activity (Scot 2026-07-19), so no licensed-clinician gate applies (see docs/legal/ANTHROPIC_BAA_ACCEPTED.md). | No customer notice required (no new subprocessor and no new data flow; contract-basis strengthening only). Privacy Office to note at the 2026-07-20 quarterly review. |
| 2026-07-22 | Gate 1 DNS cutover completed: `app.lingolinq.com` is live on GCP Cloud Run, with Cloud SQL PostgreSQL and Memorystore Redis carrying production traffic. Added Google Cloud Platform infrastructure as active subprocessor #15. Updated Render rows #2 and #10 from active primary hosting/database to superseded write-frozen rollback fallback pending decommission. Render remains listed until its production fallback data and services are deleted or restricted. (Row number reconciled to #19 on merge with the localization/speech re-scope PR, which had independently claimed #15-#18.) | No §2 customer notice fired. §2's 30-day advance notice runs to existing tenants; production carries only synthetic/test data with no real tenant onboarded (§5.7 pre-tenant baseline), so the GCP addition affects no tenant. GCP #19 is recorded as part of the disclosed subprocessor baseline the first real tenant receives at onboarding; the 30-day clock attaches to the next subprocessor change made after a tenant is live. Pre-first-tenant confirmation tracked in the compliance calendar (`gate-first-tenant-subprocessor-baseline`). |
| 2026-07-23 | Remediation landed. PR #667 repointed all six Cloud **TTS** call sites from the Pre-GA `v1beta1` endpoint to GA `v1` (`lib/tts.rb`, `app/controllers/api/search_controller.rb`) and removed the unauthenticated consumer `translate.google.com/translate_tts` fallback. All three Google localization/speech flows (#15/#16/#18) now ride GA covered-service endpoints, within the Google Cloud BAA for PHI. Row #16 and §5.8 updated from "residual open" to "remediated". At the time of this entry, still open (CEO): Abair (#17) DPA-or-disable, a per-flow COPPA-consent / feature-flag posture, and the finding triage (LL-c38e7da48e / LL-a167848115 / LL-1eb9a2435b) -- all resolved later the same day in the entry below (Abair disabled, findings triaged). | No customer notice (code hardening + doc reconciliation; no new data flow or contract change; prod still synthetic/test only per §5.7) |
| 2026-07-23 | CEO triage of the localization/speech cluster (re-attested with this amendment). **Abair Irish TTS (#17) DISABLED** in code via PR #674 -- the `ga`-locale paths in `lib/tts.rb` and `app/controllers/api/search_controller.rb#audio` short-circuit and make no request to `abair.ie` (no DPA on file; Google Cloud TTS has no Irish voice so there is no covered replacement; disabling chosen over executing a DPA). Row #17 updated from "Pending -- NO DPA" to "DISABLED"; reaches-vendor changed to "No -- disabled". Findings triaged: **LL-a167848115 (TTS) remediated** (Abair disabled + #667 GA repoint + fallback removal close every named sub-defect); **LL-c38e7da48e (#15) and LL-1eb9a2435b (#18) accepted-risk** (BAA covered services, disclosed; the raw-content auto-egress consent posture is accepted for the pre-MVP test-only window and to be revisited before real tenant onboarding). §5.8 updated from "open remediation item" to "triaged". | No customer notice (removal/disable of a flow + doc reconciliation; no new data flow; prod still synthetic/test only per §5.7). Abair disable is a customer-facing feature removal to note at the next quarterly review. |
| 2026-07-24 | Recorded the §2 subprocessor-notice position for the GCP infrastructure addition (#19) and resolved the open "record bulletin timing" item left by the 2026-07-22 cutover entry. Position (CEO): no §2 notice is owed, because §2's 30-day advance notice runs to existing tenants and production carries only synthetic/test data with no real tenant onboarded (§5.7). GCP #19 is the disclosed baseline the first real tenant receives at onboarding; the 30-day advance-notice clock attaches to the next subprocessor change made after a tenant is live. Added a `Pre-tenant baseline` clarification to §2 and a conditional pre-first-tenant confirmation gate to the compliance calendar (`gate-first-tenant-subprocessor-baseline`). Documentation/position only; no data-flow, contract, or vendor change. | No customer notice (position record; the 30-day clock is dormant until the first real tenant is live). |
| 2026-08-02 | Reclassified AI vendor row 4 (Anthropic, PBC) from an active AI flow to DORMANT, correcting the register to match runtime state, exactly as the 2026-07-12 entry did for row 5 (Gemini). "Reaches vendor" moves from "Yes, pseudonymized learner content" to "Currently no (dormant since 2026-07-30)". Cause: runtime AI routes to Anthropic Claude on AWS Bedrock via `lib/ai_client.rb`, but no `lingolinq-web` revision carries a Bedrock credential, so `AiClient.configured?` is false and no model egress occurs on any path; the prior direct `api.anthropic.com` route was removed by PR #681 and is CI-enforced. The engagement and the HIPAA-Ready BAA (2026-07-18) remain in force and the row is retained, since the path is expected to resume once credentials are mounted. This is a change of runtime status only, NOT a subprocessor removal, so the §2 30-day customer change-notice clock is not triggered. Evidence and the standard for re-verification: the 2026-08-01 correction in `docs/legal/AWS_BAA_ACCEPTED.md` (PR #725). Re-attestation owed. **[Discharged 2026-08-04: Scot Wahlquist, CEO, re-attested this document on that date. This sentence is retained as the historical record of the row. This discharge covers the obligation created by THIS row only; it is not a statement about the document's current attestation state, which is given by the Re-attestation status line in the header and governs.]** | No customer notice. Per this row's own determination above, a change of runtime status is not a subprocessor addition or removal, so section 2's 30-day advance-notice clock is not triggered. Section 2's **Pre-tenant baseline** and section 5.7 apply independently: production carried only synthetic/test data with no real tenant onboarded, so no tenant was affected and there was no tenant to notify. **[Cell completed 2026-08-06** from this row's own text; it was omitted when the row was written, and git history confirms it was never populated. The notice determination is unchanged and is not derived from any neighbouring row. See the 2026-08-06 entry below.**]** |
| 2026-08-04 | Corrected the row 4 status again. The 2026-08-02 entry above was accurate when written and expired on 2026-08-03T08:23:02Z, when revision `00013-76w` mounted `BEDROCK_AWS_KEY` / `BEDROCK_AWS_SECRET` and the Bedrock path became operational for the first time. It carried exactly one call, an internal verification call at 2026-08-04T05:44:42Z (`word_prediction`, no user attached, no user or student data), then credentials were withdrawn on 2026-08-04T06:31:46Z (revision `00014-5rw`). Row 4 now reads "not operational as of 2026-08-04" with the operational window stated explicitly, rather than "dormant since 2026-07-30". No learner content was transmitted at any point, so "Reaches vendor" remains "Currently no". Verification standard met for the first time during the window: `sts:GetCallerIdentity` under the mounted credential returned account 239044785114, principal `user/lingolinq-bedrock-runtime`. This remains a change of runtime status only, NOT a subprocessor removal, so the §2 30-day customer change-notice clock is still not triggered. Evidence: the 2026-08-04 operational-status correction in `docs/legal/AWS_BAA_ACCEPTED.md` (PR #725). Re-attestation owed. **[Discharged 2026-08-04: Scot Wahlquist, CEO, re-attested this document on that date. This sentence is retained as the historical record of the row. This discharge covers the obligation created by THIS row only; it is not a statement about the document's current attestation state, which is given by the Re-attestation status line in the header and governs.]** | No customer notice. Per this row's own determination above, this remains a change of runtime status rather than a subprocessor addition or removal, so section 2's 30-day advance-notice clock is still not triggered. The operational window stated above carried exactly one internal verification call with no user or student data, so no tenant content reached the vendor. Section 2's **Pre-tenant baseline** and section 5.7 apply independently: no real tenant was onboarded, so no tenant was affected and there was no tenant to notify. **[Cell completed 2026-08-06** from this row's own text; it was omitted when the row was written, and git history confirms it was never populated. The notice determination is unchanged and is not derived from any neighbouring row. See the 2026-08-06 entry below.**]** |
| 2026-08-06 | Record-completeness correction to this register. No subprocessor added, removed, or changed; no data flow, contract, or classification altered. Three defects fixed. (1) **Incomplete change-log field.** The 2026-08-02 and 2026-08-04 rows above were each written with only two cells, so the "Notified to customers" column was structurally absent from both, leaving an Article 28 change-log field blank rather than answered. Git history confirms the column was never populated in either row from the commit that introduced it, so this completes an omission at authoring and does not restore deleted content. Both cells are now completed from each row's own section 2 determination, read together with section 2's Pre-tenant baseline and section 5.7; neither cell is derived from a neighbouring row, and neither notice determination is changed. (2) **Stale "Last amended" date.** The header read 2026-07-23 while this change log already carried later entries dated 2026-07-24, 2026-08-02, and 2026-08-04. (3) **Unbounded open-item claim.** The header carried an undated live clause stating that Privacy Office confirmation of the customer-notice timing under section 2 was "still owed before external publication." That was true when written on 2026-07-23 and was answered on 2026-07-24, when the CEO position that no section 2 notice is owed was recorded and section 2's Pre-tenant baseline paragraph was added. The clause is now bounded to the period it was true, with the historical sentence retained, rather than left standing as an open runtime-state claim. | No customer notice. Documentation and record-completeness only: no subprocessor added, removed, or changed and no data flow altered, so section 2 is not engaged. Section 2's Pre-tenant baseline and section 5.7 apply as well: production carries only synthetic/test data with no real tenant onboarded. |
| 2026-08-07 | **Runtime-status correction (material), plus two record corrections.** No subprocessor added, removed, or changed; no data flow or contract altered. (1) **Row 4 (Anthropic) and the section 5.7 consistency note asserted that the Bedrock runtime path was not operational because "no `lingolinq-web` revision carries a Bedrock credential."** Verified against live infrastructure on 2026-08-07: the serving revision `lingolinq-web-00017-n65` (100% of traffic) mounts both `BEDROCK_AWS_KEY` and `BEDROCK_AWS_SECRET` from Secret Manager, both secrets hold an enabled version, and credentials have been continuously present since revision `00015-9l9` at 2026-08-04T07:25:08Z, only 53 minutes after the withdrawal this register recorded as the end state. The claim was therefore false from 2026-08-04T07:25:08Z, including on 2026-08-06 when this document was re-attested. **The data-flow determination does not change:** production `AiApiLog` holds a single row (id 1, 2026-08-04T05:44:42Z, `word_prediction`, no `user_global_id`, no `organization_global_id`), no row records learner content on this path, and "Reaches vendor" remains "Currently no." Two things changed about the BASIS for that answer, both now stated in the row rather than left implicit. First, it rests on the absence of recorded calls, not the absence of credentials. Second, following external review, the row now discloses that `AiApiLog` can under-record: `log_ai_call` rescues `ActiveRecord::ActiveRecordError` and returns an unsaved record (`app/models/ai_api_log.rb`), and `Flusher.flush_user_logs` destroys rows by `user_global_id` on user erasure (`lib/flusher.rb`). Durable vendor-side confirmation (CloudWatch `AWS/Bedrock` `Invocations` or CloudTrail `bedrock:InvokeModel`) was attempted on 2026-08-07 and could NOT be obtained: the available IAM principal `lingolinq-app` is denied `cloudwatch:GetMetricStatistics`. Granting a read-only principal that permission, and reconciling vendor-side invocation counts against `AiApiLog`, is recorded here as an open item. This is an accuracy defect in the register, not a data-protection incident. The same superseded claim appears in five other documents under `docs/legal/` and in `audit-reports/CAPABILITY-LEDGER.json`, whose `aws-s3-baa` row is held at `partial` on this premise; those are tracked separately and are NOT corrected here. (2) Header revision-chain correction. When the 2026-08-06 revision above inserted its new "Last amended" block, it retitled the retained 2026-07-23 block "Prior amendment", which asserted that the amendment immediately preceding 2026-08-06 was 2026-07-23. That skipped three amendments this change log already recorded: 2026-07-24, 2026-08-02 and 2026-08-04, the last of which was itself a CEO re-attestation. It also contradicted the header's own "Last reviewed" line, which named 2026-08-04 twenty-six lines earlier, so an auditor reading only the header would have concluded the register went from 2026-07-23 straight to 2026-08-06. The header now carries an explicit most-recent-first chain naming 2026-08-04 as the immediately preceding amendment, with the 2026-07-23 Abair/TTS narrative retained verbatim and relabelled as earlier history. Labels and ordering only; no narrative was rewritten and no date in the change log changed. Noted for the record: this defect is the same class as defect (2) in the 2026-08-06 entry above, reintroduced one line below the fix for it, and it was caught by external review rather than by any automated check, because none of the artifact-integrity checks read prose. (3) Review cadence reconciled across all three scheduling sources. This document said quarterly (next 2026-10-20), its document-register row carried an annual `nextReviewDue` of 2027-07-23, and the compliance calendar item `rev-gdpr-subprocessor-quarterly` carried 2026-09-01 with a null `lastDone`, so an operator got a different deadline depending on which source they consulted. The CEO confirmed quarterly on 2026-08-07; the register row and the calendar item are both set to 2026-10-20, and the calendar's `lastDone` is anchored to the last full line-by-line review of 2026-04-20. **That anchoring surfaces a missed cycle:** quarterly from 2026-04-20 fell due 2026-07-20 and was not performed, and both prior candidate dates skipped it. The gap is recorded in the calendar item rather than erased. The dated amendments in this change log are per-change updates, not the periodic full-register review that cadence tracks. Also corrected in the same revision: `docs/legal/AI_DATA_FLOW_CLASSIFICATION.md` carried the same superseded Bedrock claim and described `AI_GOVERNANCE_MEMO.md` as "attested 2026-06-19" when that memo had been re-attested four times since. | No customer notice. Documentation and record-completeness only: no subprocessor added, removed, or changed and no data flow altered, so section 2 is not engaged. Section 2's Pre-tenant baseline and section 5.7 apply as well: production carries only synthetic/test data with no real tenant onboarded. |
| 2026-08-08 | **Quarterly subprocessor review PERFORMED, and it found two omissions.** This is the periodic full-register review GDPR Article 28 expects, not a per-change amendment. It is the cycle that fell due 2026-07-20 and was not performed; rather than re-date the schedule past the gap, the review was carried out (CEO decision 2026-08-08). Method: every external host referenced in `lib/`, `app/models/`, `app/controllers/` and `config/` was enumerated and reconciled against the section 4 table. **Result: two active third-party processors are absent from this register.** (1) **Stripe, Inc.**, the live payment path (`lib/purchasing.rb` sets `Stripe.api_key` and retrieves Customers and Charges; `gem 'stripe'` is in the Gemfile), receiving payment and customer-identity data. Stripe was named nowhere in this document before today. (2) **iplocate.io**, receiving a user's IP address for geolocation from two call sites: `lib/external_tracker.rb`, gated the same way the HubSpot flow is, and `app/mailers/subscription_mailer.rb#new_subscription`, which has **no user-type check and no consent gate** and so can fire for any account type including a child account. Both are recorded in new section 4.1 on identification rather than held until full rows are drafted, so the table is not mistaken for a complete inventory meanwhile. Drafting rows requires DPA verification for each vendor, which is separate tracked work. The ungated iplocate call site is raised as findings-register entry **LL-07f1869d92** for CEO triage. Neither omission is new behaviour; both code paths predate this review, and both are the same defect class as the three localization/speech flows recorded 2026-07-21 and the Speech-to-Text flow recorded 2026-07-22, which is exactly what a periodic review exists to catch. `lastDone` for `rev-gdpr-subprocessor-quarterly` is set to 2026-08-08 on the strength of this review and the next cycle scheduled 2026-11-08. | No customer notice. This entry records a review and two register defects; it introduces no new data flow, vendor, or contract. Section 2's Pre-tenant baseline and section 5.7 apply: production carries only synthetic/test data with no real tenant onboarded. The two identified processors will need to appear in the disclosed baseline the first real tenant receives at onboarding. |
| 2026-08-16 | **The AI egress determination changes from "no" to "yes". Delivered as a Path A supersession** (this dated record) rather than an in-place edit, because the predecessor's bytes are pinned by the 2026-08-08 attestation and `docs/legal/README.md` rules 3-4 make an attested legal file immutable in bytes, filename, and location. The predecessor stated the Anthropic/Bedrock path was "credentialled but carrying no traffic, verified 2026-08-07", cited serving revision `lingolinq-web-00017-n65`, and recorded `AiApiLog` as holding "exactly one row". Those statements were accurate on 2026-08-07 and were overtaken by the **2026-08-12 production deploy of PR #734**, which routed runtime AI over the classic Bedrock plane and made the feature live. Verified against live production 2026-08-16: serving revision `lingolinq-web-00020-per` at 100% traffic with both Bedrock secrets mounted; `AiApiLog` holds 64 rows, 63 of them written since the 2026-08-12 deploy, 63 carrying a `user_global_id`, all successful, all on `us.anthropic.claude-haiku-4-5-20251001-v1:0` (57 `word_prediction`, 7 `board_generation`, 17,221 prompt tokens), from 2 distinct accounts of the 34 then in production. Row 4's "Reaches vendor" therefore moves from "Currently no" to **"Yes"**, and section 5.7 is corrected to match. **This is the third correction to the same cell in nine days** (2026-08-04, 2026-08-07, 2026-08-16), and the recurrence is recorded rather than quietly fixed a third time: each correction described a runtime state that a later deploy invalidated, and nothing in CI compares this register's runtime claims against the running service, so the register drifts silently every time the AI path changes. Closing that gap is separate tracked work; until it exists, treat any runtime-status sentence in this register as true only as of its stated verification date. Also recorded: `article_50_disclosure_shown` is true on all 63 post-deploy rows; `pii_detected` is false and `ip_address` is null on all 64; and `jurisdiction` is **null on all 64 rows**, so the EU retention purge matches nothing — a pre-existing gap, noted here because this register is where the egress is disclosed. A dormant `ANTHROPIC_API_KEY` mount remains on the serving revision (removal tracked in PR #805). No vendor was added or removed and no contract changed; the HIPAA-Ready BAA of 2026-07-18 governs throughout. | No customer notice. This is a change of runtime status, not a subprocessor addition or removal, so section 2's 30-day advance-notice clock is not triggered; Anthropic has been a listed subprocessor under an executed BAA throughout. Section 2's **Pre-tenant baseline** applies independently: production had no real tenant onboarded (section 5.7), so no tenant was affected and there was none to notify. **This determination must be revisited before the first real tenant is onboarded** — from that point this is a live learner-content flow to a third-country processor, and the disclosed-baseline-at-onboarding obligation in section 2 attaches to it. Tracked in the compliance calendar under `gate-first-tenant-subprocessor-baseline`. |

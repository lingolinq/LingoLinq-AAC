# LingoLinq Capability Ledger

> Generated from `audit-reports/CAPABILITY-LEDGER.json` by `scripts/capability-check.rb --render`.
> Do not hand-edit; edit the JSON (the source of truth) and re-render.
>
> **Status: canonical.** `built`/`partial` rows' `currentEvidence` is
> validated to resolve at HEAD, and `deliberately-not-done` rows' `negativeEvidence` is
> enforced, by `scripts/capability-check.rb --check` in CI (audit-artifacts-integrity). A
> capability is a present-tense claim: if the backing code is removed, the check goes red.
> Verified against `staging`; generated 2026-07-12.

## Built (12)

| Capability | Evidence (HEAD) | Anti-claim / note |
|---|---|---|
| Password authentication (bcrypt-hashed, via the passwords concern / GoSecure) | `app/models/user.rb:108` | NOT passwordless / device-JWT-only. |
| Server-side at-rest encryption of sensitive fields (secure_serialize / SECURE_ENCRYPTION_KEY) | `app/models/concerns/secure_serialize.rb:1` | NOT end-to-end encryption; LingoLinq holds the keys. Do not claim 'zero readable keys' or E2EE. |
| PiiScrubber pseudonymizes (scrubs direct identifiers from) payloads before AI egress | `lib/pii_scrubber.rb:215` | NOT de-identified or anonymized. Pseudonymized data is still personal data; does not meet HIPAA Safe Harbor. |
| COPPA hard-gate blocks AI generation for under-13 users awaiting parental consent | `lib/ai_board_generator.rb:58` | This is an under-13 (COPPA) gate; it does NOT itself enforce an EU under-16 (GDPR Art. 8) block on the AI path (see cap eu-under16-ai-block). |
| Jurisdiction detection primitive (EU vs non-EU) exists | `app/models/lingo_linq/jurisdiction.rb:58` | The EU jurisdiction primitive gates the Art. 50(1) disclosure modal (built and staged; the article_50_disclosure flag is AVAILABLE-only in CODE but is ENABLED in PRODUCTION via the default_enabled_features DB Setting, verified 2026-08-23 -- see cap art50-1-disclosure-modal and docs/legal/2026-08-23_article-50-production-flag-verification.md) and, via the Phase 4 call-context helper, stamps jurisdiction on AiApiLog rows; it does not by itself drive an under-16 AI block. |
| Full hard-delete of a user on request (flush_user_completely) | `lib/flusher.rb:418` | There is no automatic time-based wipe (no 48h/180d purge); deletion is on request / policy-driven. |
| AiApiLog audit trail with scrubbed summary columns / IP redaction | `app/models/ai_api_log.rb:30` | Redaction is pseudonymization of the log record, not anonymization. |
| Message banking retains the user's OWN voice recordings for playback | `app/models/button_sound.rb:11` | These are the user's own communication recordings, NOT voiceprints/speaker-ID/biometrics (see cap no-voiceprints). |
| Signed AWS Business Associate Agreement (HIPAA-eligible AWS services in use) | `docs/legal/AWS_BAA_ACCEPTED.md:65` | Do NOT claim Bedrock inference was re-verified on any revision after lingolinq-web-00020-per (swept 2026-08-16); credential mounts are non-monotonic. The BAA covers Bedrock; last checked operational mount is 00020-per. One word-prediction call completed during the closed window 2026-08-03T08:23Z to 2026-08-04T06:31Z on 00013-76w, so do NOT state this as 'never'; see docs/legal/2026-08-24_ai-governance-memo.md. Do NOT cite the retracted 2026-07-27 claim as evidence that the operative condition (calls signed under account 2390-4478-5114) was verified; it was false when made and stays retracted. The condition WAS verified once, on 2026-08-04, during the closed 00013-76w window (sts:GetCallerIdentity returned 2390-4478-5114, principal user/lingolinq-bedrock-runtime). Cite that dated verification, not the retracted one. The AWS BAA does NOT cover a direct third-party model endpoint outside AWS (e.g. api.anthropic.com). That direct path is unused at runtime today; when used it is covered by the Anthropic HIPAA-Ready BAA (see cap anthropic-model-provider-baa). Fable/Mythos models are excluded from Bedrock HIPAA eligibility. |
| EU AI Act Article 50(1) user-facing AI-interaction disclosure modal | `app/frontend/app/components/ai-disclosure.js:8` | Built and staged. In CODE the `article_50_disclosure` flag is registered in AVAILABLE_FRONTEND_FEATURES only, but production overrides that via the default_enabled_features DB Setting and the flag IS ENABLED there (verified 2026-08-23). Claims about this capability must state which layer they describe: 'not in ENABLED_FRONTEND_FEATURES' is a statement about code, NOT about production. Enabling the flag was the 2026-08-02 release gate, on Scot's sign-off, after the production deploy. GATE STATUS CORRECTED AGAIN 2026-08-23 - THE FLAG IS ENABLED IN PRODUCTION. A direct read of production (see docs/legal/2026-08-23_article-50-production-flag-verification.md) found article_50_disclosure PRESENT in the default_enabled_features DB Setting, and FeatureFlags.feature_enabled_for? resolving true for all 34 of 34 users at 2026-08-23T21:04:12Z. The DB Setting overrides the code constant, which a reading of AVAILABLE_FRONTEND_FEATURES alone cannot show - that is how the earlier 'shown to no one' assessment went wrong. The enabled-SINCE date is NOT recoverable (Setting has no version history), so the historical window below stands as recorded and is NOT re-derived from the new read. The production deploy occurred (revision 00012-x8z, 2026-08-02T20:31Z) and runtime AI subsequently became operational on revision 00013-76w (2026-08-03T08:23Z to 2026-08-04T06:31Z). During that ~22h window the product was performing AI interactions with the Article 50(1) disclosure disabled. Exposure was limited: the only call in the window was an internal verification call with no end user involved, and credentials have since been withdrawn, so no user encountered an undisclosed AI interaction. This gate is now OVERDUE and must be closed before any future credential mount, not after. Enabling a user-facing flag is Scot's decision and is deliberately not auto-applied here. [CORRECTED 2026-08-25: the 'now OVERDUE and must be closed' clause is stale and contradicted the same field's own 'THE FLAG IS ENABLED IN PRODUCTION' statement. The 2026-08-02 gate's OUTCOME is satisfied in the current production Setting; whether it was discharged by the explicit sign-off the gate required is unrecoverable, since Setting carries no version history.] |
| Jurisdiction-aware under-16 (EU GDPR Art. 8) block on the AI generation path | `lib/feature_flags.rb:266` | This gate is NOT the same as signup COPPA (settings['coppa']) or second-tier AI data-sharing VPC (settings['ai_consent'] / government-ID). It only covers EU under-16 AI enablement. |
| Signed BAA covering the AI model-provider (Anthropic) egress path | `docs/legal/ANTHROPIC_BAA_ACCEPTED.md:144` | Do NOT claim a later revision than lingolinq-web-00020-per was checked. Do NOT cite the retracted 2026-07-27 claim as evidence the Bedrock operative condition was verified; that claim was false when made and was retracted. The condition WAS independently verified for the first time on 2026-08-04 (sts:GetCallerIdentity returning 239044785114 under the mounted credential), during the closed 00013-76w window only; cite that dated verification rather than the retracted one. Google (Gemini) as a model provider still has no BAA; its runtime fallback remains disabled (2026-07-09). The Anthropic BAA is the still-available direct-path basis, not the active Bedrock route. |

## Partial (4)

| Capability | Evidence (HEAD) | Anti-claim / note |
|---|---|---|
| TOTP two-factor authentication (ROTP), currently OPTIONAL | `app/models/concerns/passwords.rb:95` | NOT mandatory; do not claim enforced MFA for all users or admins. |
| EU AI Act Article 50(2) machine-readable output marking -- board-generation slice only | `lib/ai_board_generator.rb:149` | The Art. 50(2) obligation is NOT closed: this slice marks board generation ONLY. Other AI-output surfaces (generate_focus_words, AiWordPredictor.predict, eval narration, AiPredictionGenerator) are not yet marked, and durable persistence of the marker (board.settings + relinking copy_for) is follow-up. Also distinct from the Art. 50(1) disclosure modal (see cap art50-1-disclosure-modal). |
| AI board generation + word prediction via Anthropic Claude on AWS Bedrock (Anthropic-only runtime); last verified operational on lingolinq-web-00020-per (2026-08-16); later revisions unchecked | `lib/ai_word_predictor.rb:336` | Do NOT claim a later revision than lingolinq-web-00020-per was checked; credential mounts are non-monotonic. Do NOT claim eval narration is operational. Do NOT say these features have never been operational: the path ran 2026-08-03T08:23Z to 2026-08-04T06:31Z on 00013-76w. Do NOT say all features 'ran' in that first window: only word prediction completed a call then. The prior Google Gemini fallback was DISABLED 2026-07-09; there is no runtime Gemini path. When configured, runtime AI egresses to Claude on AWS Bedrock via lib/ai_client.rb, not the direct api.anthropic.com endpoint; enforced in CI by scripts/ai-endpoint-guard.sh. Only pseudonymized data is sent (see cap pii-scrubber-pseudonymization). |
| Zero-data-retention (ZDR) confirmed for the two in-scope Anthropic models ON THE DIRECT api.anthropic.com PATH ONLY (not the Bedrock runtime route) | `docs/legal/AI_DATA_SHARING_CONSENT.md:79` | Do NOT cite this row as a ZDR guarantee for the AWS Bedrock runtime route; it is scoped to the direct api.anthropic.com path, which is not in runtime use. The shipped runtime disclosure (privacy.hbs, lib/lingo_linq/ai_consent_disclosures.rb) explicitly states that no zero-data-retention guarantee is claimed for the Bedrock path, and this ledger row must not contradict it. ZDR is confirmed ONLY for these two specific models; it does NOT extend to any other/future Anthropic model outside the ZDR-eligible tier, and NOT to any non-Anthropic provider. ZDR is NOT a substitute for a HIPAA BAA (see cap anthropic-model-provider-baa for the executed Anthropic BAA and cap aws-s3-baa for Bedrock under the AWS BAA). Fable 5 / Mythos-class models are explicitly NOT ZDR-eligible and never receive identifiable payloads. |

## Deliberately not done -- out-of-scope by design (3)

| Capability | Negative-evidence scope | Expected |
|---|---|---|
| End-to-end encryption / zero-readable-keys | app/**/*.rb, lib/**/*.rb for end_to_end/e2ee/zero_knowledge/client_side_encrypt | 0 matches |
| Voiceprints / speaker-identification / voice-cloning of training audio | app/**/*.rb, lib/**/*.rb for voiceprint/speaker_id/speaker_recognition/voice_clone | 0 matches |
| Diagnosis / disability / IEP / 504 / medical-condition data fields | db/schema.rb for disabilit/diagnos/\biep\b/\b504\b/impair/medical_condition | 0 matches |

---

*Framing: "deliberately not done" means out-of-scope by design, never a known gap shipped without. These rows exist to stop over-claims, mirroring the "Deliberately not claimed" section of `COMPLIANCE_PROGRAM_OVERVIEW.md`.*
